jdk:
  - openjdk11

before_install:
  - yes | sdkmanager --licenses
  - sdkmanager "platforms;android-37" "build-tools;37.0.0"

# Fix: This must be a raw string command, not a list item
build: ./gradlew :GooglePay:assembleRelease