<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- This sets the ripple color, you can use a custom color here -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/paypal_blue" /> <!-- This defines the shape of the ripple mask -->
        </shape>
    </item>
    <item android:drawable="@color/paypal_blue" /> <!-- The background of your custom view -->
</ripple>
